# Проверка настройки домена Telegram бота

## Проблема: "Bot domain invalid"

Эта ошибка означает, что Telegram виджет не может загрузиться, потому что домен не принят Telegram.

## Решения:

### 1. Проверьте настройку в BotFather:

Выполните в @BotFather:
```
/setdomain
@S7b0t4bot
cards.sybota.space
```

**Важно:**
- Telegram может требовать время (до 10-15 минут) для применения изменений
- Домен должен быть доступен по HTTPS
- SSL сертификат должен быть валидным

### 2. Проверьте доступность домена:

```bash
curl -I https://cards.sybota.space
```

Должен вернуть HTTP 200.

### 3. Проверьте в браузере:

1. Откройте https://cards.sybota.space/auth
2. Откройте консоль браузера (F12)
3. Проверьте, нет ли ошибок загрузки Telegram виджета
4. Проверьте, что виджет пытается загрузиться с правильным доменом

### 4. Временно отключена проверка хеша:

Для отладки проверка хеша временно отключена (`SKIP_TELEGRAM_HASH_CHECK=true`).

После того, как домен заработает, включите обратно:
```bash
cd ~/proj
sed -i 's/SKIP_TELEGRAM_HASH_CHECK=true/SKIP_TELEGRAM_HASH_CHECK=false/' backend/.env
docker-compose restart backend
```

### 5. Альтернатива - используйте тестовый аккаунт:

Пока домен не настроен, можно использовать кнопку "Войти как тестовый аккаунт" на странице авторизации.

## Что проверить:

1. ✅ Домен `cards.sybota.space` указан в BotFather
2. ⏳ Подождите 10-15 минут после настройки
3. 🔄 Очистите кеш браузера (Ctrl+Shift+Del)
4. 🔄 Попробуйте в режиме инкогнито
5. ✅ Проверьте, что SSL сертификат валиден

## Если не помогает:

1. Попробуйте использовать основной домен `sybota.space` (если он настроен)
2. Или используйте тестовый аккаунт для входа


# Финальный чеклист настройки

## ✅ Что уже сделано:

1. ✅ Nginx установлен и настроен
2. ✅ Конфигурация для sybota.space создана
3. ✅ Nginx правильно проксирует запросы на приложение
4. ✅ Приложение работает (frontend на 3003, backend на 3002)

## 🔍 Проверьте работу:

### 1. Проверьте доступность сайта извне:

Откройте в браузере: **http://sybota.space**

Должна открыться страница авторизации.

### 2. Проверьте API:

```bash
curl http://sybota.space/api
```

Должен вернуться ответ от backend.

### 3. Настройте Telegram бота:

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/setdomain`
3. Выберите вашего бота из списка
4. Введите домен: `sybota.space` (без http:// или https://)
5. Подтвердите

**Важно:** После настройки домена в BotFather может потребоваться несколько минут для применения.

### 4. Проверьте переменные окружения:

Убедитесь, что в `docker-compose.yml` или `.env` указаны правильные URL:

```env
FRONTEND_URL=http://sybota.space
NEXT_PUBLIC_API_URL=http://sybota.space
```

Если нужно обновить:

```bash
cd ~/proj
docker-compose down
docker-compose up -d --build
```

### 5. Проверьте работу авторизации:

1. Откройте http://sybota.space/auth
2. Должна появиться кнопка "Login with Telegram"
3. Нажмите на неё и проверьте авторизацию

## 🔒 Настройка SSL (HTTPS) - рекомендуется:

После проверки работы на HTTP, настройте HTTPS:

```bash
# Установите certbot
sudo pacman -S certbot certbot-nginx

# Получите сертификат
sudo certbot --nginx -d sybota.space -d www.sybota.space
```

Certbot автоматически:
- Получит SSL сертификат от Let's Encrypt
- Обновит конфигурацию nginx
- Настроит автоматическое обновление сертификата

После получения сертификата обновите переменные окружения:

```bash
cd ~/proj
docker-compose down
FRONTEND_URL=https://sybota.space NEXT_PUBLIC_API_URL=https://sybota.space docker-compose up -d --build
```

И обновите домен в BotFather на `https://sybota.space` (хотя обычно достаточно просто `sybota.space`).

## 📝 Полезные команды:

```bash
# Проверка статуса nginx
sudo systemctl status nginx

# Просмотр логов nginx
sudo tail -f /var/log/nginx/sybota.space.access.log
sudo tail -f /var/log/nginx/sybota.space.error.log

# Проверка статуса приложения
docker-compose ps

# Просмотр логов приложения
docker-compose logs -f frontend
docker-compose logs -f backend

# Перезапуск nginx
sudo systemctl reload nginx

# Перезапуск приложения
docker-compose restart
```

## 🎉 Готово!

Если всё работает, ваш сайт доступен по адресу: **http://sybota.space**

После настройки SSL: **https://sybota.space**

